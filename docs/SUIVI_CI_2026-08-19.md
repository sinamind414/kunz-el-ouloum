# Suivi CI — 2026-08-19

Journal de la remise en service de la CI (suite à l'audit d'architecture du 19/08).

## État

| Élément | Statut |
|---|---|
| `typecheck-build-test` sur la PR #2 | ✅ **vert** (1re fois depuis 4+ jours) : typecheck, 643/643 tests, golden terms, formatage, build 59 chunks + `checkDist`, `npm audit` = 0 |
| Job `e2e` | ⏳ en cours — l'installation de Chromium (`npx playwright install --with-deps chromium`) depuis `cdn.playwright.dev` est lente/instable (même coupure TLS constatée depuis l'environnement local) |
| Correctif CI préparé | commit local `3bd6753` : cache `~/.cache/ms-playwright` (actions/cache), 3 tentatives d'installation, `timeout-minutes` 15 (install) / 30 (E2E). **Push rejeté** par GitHub : l'App de cette session n'a pas la permission `workflows` |

## Correctifs CI à pousser dès que la permission `workflows` sera accordée

```yaml
# .github/workflows/ci.yml — job e2e
- name: Cache Playwright browsers
  uses: actions/cache@v4
  with:
    path: ~/.cache/ms-playwright
    key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

- name: Install Playwright Chromium
  timeout-minutes: 15
  env:
    PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT: 120000
  run: |
    for i in 1 2 3; do
      if npx playwright install --with-deps chromium; then
        echo "✅ Chromium installé (tentative $i)"
        exit 0
      fi
      echo "⚠️ Échec de l'installation ($i/3) — nouvelle tentative…"
      sleep 10
    done
    echo "❌ Installation de Chromium impossible après 3 tentatives"
    exit 1

- name: Run E2E
  timeout-minutes: 30
  run: npm run test:e2e
```

## Liste des causes d'échec CI résolues dans cette session

1. **7 tests unitaires rouges** (boussole V3 `MyPathView` absente, visuels `LessonsView` retirés) → implémentés/restaurés (`04a49f2`).
2. **Timeout 5 s** sur `InteractiveLessonView.transcriptionSprint.test.tsx` (runners 2 vCPU plus lents que la machine locale) → `testTimeout: 15000` (`a7df36f`).
3. **3 occurrences de « وARNt »** sans espace dans `src/data/activeLessons.ts` (étape formatting jamais atteinte auparavant) → corrigées (`a321e6f`).
4. **Espace de fin de ligne** dans `docs/AUDIT_ARCHITECTURE_SVT_BAC_2026-08-19.md` (étape `git diff --check`) → corrigée (`b7346b2`).
5. **Installation Chromium E2E** → correctif en attente de la permission `workflows` (ce fichier).
