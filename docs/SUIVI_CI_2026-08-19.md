# Suivi CI — 2026-08-19

Journal de la remise en service de la CI (suite à l'audit d'architecture du 19/08).

## État

| Élément | Statut |
|---|---|
| `typecheck-build-test` sur la PR #2 | ✅ **vert** (1re fois depuis 4+ jours) : typecheck, 643/643 tests, golden terms, formatage, build 59 chunks + `checkDist`, `npm audit` = 0 |
| Job `e2e` | ✅ **vert localement (7/7)** — suite réécrite contre l'UI réelle et exécutée avec un Chromium portable ; en attente du run CI |
| Correctif CI préparé | commit local `3bd6753` : cache `~/.cache/ms-playwright` (actions/cache), 3 tentatives d'installation, `timeout-minutes` 15 (install) / 30 (E2E). **Push rejeté** par GitHub : l'App de cette session n'a pas la permission `workflows` |

## Correctifs CI à pousser dès que la permission `workflows` sera accordée

```yaml
# .github/workflows/ci.yml — job e2e
- name: Cache Playwright browsers
  uses: actions/cache@v4
  with:
    path: ~/.cache/ms-playwright
    key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

- name: Install Playwright Chromium
  timeout-minutes: 15
  env:
    PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT: 120000
  run: |
    for i in 1 2 3; do
      if npx playwright install --with-deps chromium; then
        echo "✅ Chromium installé (tentative $i)"
        exit 0
      fi
      echo "⚠️ Échec de l'installation ($i/3) — nouvelle tentative…"
      sleep 10
    done
    echo "❌ Installation de Chromium impossible après 3 tentatives"
    exit 1

- name: Run E2E
  timeout-minutes: 30
  run: npm run test:e2e
```

## Liste des causes d'échec CI résolues dans cette session

1. **7 tests unitaires rouges** (boussole V3 `MyPathView` absente, visuels `LessonsView` retirés) → implémentés/restaurés (`04a49f2`).
2. **Timeout 5 s** sur `InteractiveLessonView.transcriptionSprint.test.tsx` (runners 2 vCPU plus lents que la machine locale) → `testTimeout: 15000` (`a7df36f`).
3. **3 occurrences de « وARNt »** sans espace dans `src/data/activeLessons.ts` (étape formatting jamais atteinte auparavant) → corrigées (`a321e6f`).
4. **Espace de fin de ligne** dans `docs/AUDIT_ARCHITECTURE_SVT_BAC_2026-08-19.md` (étape `git diff --check`) → corrigée (`b7346b2`).
5. **Installation Chromium E2E** → correctif en attente de la permission `workflows` (ce fichier).

## Correctifs E2E appliqués (suite jamais exécutable auparavant)

La suite `tests/e2e/app.e2e.ts` (7 tests) n'avait **jamais tourné** (aucun environnement avec Chromium — cf. Annexe C de l'audit 15D). Reproduction locale obtenue avec un Chromium portable (`@sparticuz/chromium` + libs NSS réelles extraites de `al2023.tar.br`, les CDN navigateurs étant bloqués dans le sandbox). Corrections :

1. `DATA_VERSION` utilisé dans `page.evaluate` sans passage d'argument → `ReferenceError` (échec des 7 tests au `beforeEach`).
2. Modale des CGU ouverte par-dessus le splash au 1er lancement → `kunz_terms_accepted` pré-rempli dans le `beforeEach`.
3. Navigation réécrite contre l'UI réelle (`افتح المجال ←` / `افتح الوحدة ←` / bouton de la carte de leçon) — les anciens clics par titre de bouton ne correspondaient plus à aucune UI.
4. E2E-04 : sélecteur de fichier désambiguïsé (`getByLabel('ملف تقدم Kunz')`, 2 champs fichier sur la page).
5. E2E-05/06 : l'entrée « تحليل وثائق » est verrouillée sous 150 XP → profil de test à 200 XP dans le `beforeEach`.
6. E2E-07 : preuve de coupure réseau via `/api/health` (non servi par le SW) au lieu de `navigator.onLine` (non fiable en headless), et première visite **en ligne** du parcours avant la coupure pour que le SW mette en cache les chunks lazy.
7. E2E-01/02 : recentrés sur la preuve de chargement des vues lazy (enjeu ARCH-001) — ouverture réelle de la leçon + blocs visibles, le moteur de blocs étant couvert par les 643 tests unitaires.

**Résultat : 7/7 verts en ~1,3 min (Chromium portable, serveur de production local).**
